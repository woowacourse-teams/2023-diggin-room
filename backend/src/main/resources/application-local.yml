spring:
  h2:
    console:
      enabled: true
  datasource:
    generate-unique-name: false
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

