<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DigginRoom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DigginRoom" parent="Base.Theme.DigginRoom" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen.IconBackground">
        <!-- Splash 화면의 백그라운드 컬러 지정 -->
        <item name="windowSplashScreenBackground">@color/indigo</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.DigginRoom</item>
    </style>
</resources>
